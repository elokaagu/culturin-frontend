/** Public Supabase Storage bucket for web uploads; create via migration 004 (HEIC: 007). */
export const SUPABASE_PUBLIC_MEDIA_BUCKET = "media" as const;

/** Public PDF bucket for Studio sales decks; create via migration 038. */
export const SUPABASE_SALES_DECKS_BUCKET = "sales-decks" as const;
